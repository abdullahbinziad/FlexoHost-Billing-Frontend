/**
 * Client Service Layer
 * Abstracts API calls for client panel
 */

import { clientApi } from "@/store/api/clientApi";

// Example service functions
// These will be implemented when backend is ready

export const clientService = {
  // getServices: () => clientApi.useGetClientServicesQuery(),
  // createTicket: (data: CreateTicketRequest) => clientApi.useCreateTicketMutation(),
  // ... other client operations
};
